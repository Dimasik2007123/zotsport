name: CI/CD
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
            username: ${{secrets.DOCKER_USERNAME}}
            password: ${{secrets.DOCKER_PASSWORD}}
      - name: Build and push frontend
        uses: docker/build-push-action@v6
        with:
          context: .
          file: frontend/Dockerfile
          push: true
          tags: ${{secrets.DOCKER_USERNAME}}/zotsport-frontend:latest
      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker-backend/Dockerfile
          push: true
          tags: ${{secrets.DOCKER_USERNAME}}/zotsport-backend:latest
      - name: Build and push sqlite
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker-sqlite/Dockerfile
          push: true
          tags: ${{secrets.DOCKER_USERNAME}}/zotsport-sqlite:latest
      - name: Build and push proxy
        uses: docker/build-push-action@v6
        with:
          context: .
          file: proxy/Dockerfile
          push: true
          tags: ${{secrets.DOCKER_USERNAME}}/zotsport-proxy:latest
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{secrets.SERVER_IP_NEW}}
          username: ${{secrets.SERVER_USER}}
          key: ${{secrets.SSH_KEY}}
          script: |
            cd /root/zotsport/curs3
            export DOCKER_USERNAME=${{secrets.DOCKER_USERNAME}}
            docker pull ${{secrets.DOCKER_USERNAME}}/zotsport-frontend:latest
            docker pull ${{secrets.DOCKER_USERNAME}}/zotsport-backend:latest
            docker pull ${{secrets.DOCKER_USERNAME}}/zotsport-sqlite:latest
            docker pull ${{secrets.DOCKER_USERNAME}}/zotsport-proxy:latest
            docker stack deploy -c docker-compose.yaml zotsport
            sleep 10
            docker service ls
